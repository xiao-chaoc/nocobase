# Car Rental Runtime Modification Items

当前阶段：Runtime / 服务 / 动作注册 Codex-only dry-run 已建立。当前不要求用户本地运行；正式版前才本地执行。production_ready=false。不启用真实 IOPGPS，mock 数据不能进入生产。

| 编号 | runtime 项 | 状态 | 业务规则 | 涉及插件 | 涉及文件 | 是否阻塞 UAT | 是否阻塞生产 | Codex 修改建议 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RT-001 | 合同创建 runtime | planned | 合同创建需校验司机、车辆、长租/时限合同规则，激活后才能生成台账。 | plugin-rental-core | `packages/plugins/plugin-rental-core/src/server/pluginRegistration.ts`, `packages/plugins/plugin-rental-core/src/server/actions/actionRegistry.ts`, `packages/plugins/plugin-rental-core/src/server/services/contractLifecycleService.ts` | 是 | 是 | 将 action registry 中的合同激活/创建草案接入真实 service/action 注册，并补权限校验。 | dry-run 可识别入口；pre-release 本地验证合同创建不会绕过车辆冲突和合同规则。 | codex_dry_run_created |
| RT-002 | 租金台账生成 runtime | planned | 长租和时限合同按自然周生成每日租金台账，默认免租日生效。 | plugin-rental-core | `packages/plugins/plugin-rental-core/src/server/services/ledgerGenerationService.ts`, `packages/plugins/plugin-rental-core/src/server/schedules/scheduleRegistry.ts` | 是 | 是 | 将台账生成 service 注册为动作和 scheduler/workflow，并补幂等验证。 | 每日台账为唯一事实来源；长租未来台账可补生成；时限合同覆盖合同期。 | codex_dry_run_created |
| RT-003 | 付款分配 runtime | planned | 付款必须按日分配，单日不可超付，分配失败整笔失败。 | plugin-rental-core | `packages/plugins/plugin-rental-core/src/server/services/paymentAllocationService.ts`, `packages/plugins/plugin-rental-core/src/server/actions/actionRegistry.ts` | 是 | 是 | 将付款确认、冲正动作接入真实 action，并补单日超付测试。 | dry-run 识别付款入口；pre-release 验证超付被拒绝且不产生半成功数据。 | codex_dry_run_created |
| RT-004 | 押金处理 runtime | planned | 押金收取、抵扣、退还；押金不计入租金收入。 | plugin-rental-core | `packages/plugins/plugin-rental-core/src/server/services/depositService.ts`, `packages/plugins/plugin-rental-core/src/server/collections/depositRecords.ts` | 是 | 是 | 将押金生命周期动作接入真实 service/action，并补财务汇总隔离。 | 押金记录可追踪，抵扣与退款不污染租金收入。 | codex_dry_run_created |
| RT-005 | 欠款计算 runtime | planned | 欠款计算保留未付原因，当前欠款不包含未来应收。 | plugin-rental-core | `packages/plugins/plugin-rental-core/src/server/services/shortfallService.ts`, `packages/plugins/plugin-rental-core/src/server/services/unpaidReasonService.ts` | 是 | 是 | 将欠款计算和未付原因动作接入真实 runtime，并补未来应收排除测试。 | 当前欠款只统计截至当前日期应收未收。 | codex_dry_run_created |
| RT-006 | 合同文档生成 runtime | planned | 合同文档生成、打印、签署扫描件上传；当前不生成真实合同文件。 | plugin-contract-documents | `packages/plugins/plugin-contract-documents/src/server/pluginRegistration.ts`, `packages/plugins/plugin-contract-documents/src/server/services/contractDocumentService.ts`, `packages/plugins/plugin-contract-documents/src/server/actions/actionRegistry.ts` | 是 | 是 | 后续合同文件测试阶段接入真实文件生成和扫描件 mock 校验。 | Codex-only 不生成真实合同；pre-release 验证模板、输出和签署扫描件流程。 | codex_dry_run_created |
| RT-007 | GPS mock sync runtime | planned | IOPGPS mock sync 可验证状态归一化和错误日志；真实同步默认禁用。 | plugin-iopgps | `packages/plugins/plugin-iopgps/src/server/pluginRegistration.ts`, `packages/plugins/plugin-iopgps/src/server/actions/actionRegistry.ts`, `packages/plugins/plugin-iopgps/src/server/schedules/scheduleRegistry.ts` | 否 | 是 | 保持 `callsRealApi=false` 和 `defaultEnabled=false`，后续 GPS mock 阶段补 mock 数据。 | dry-run 不调用真实 IOPGPS；发现真实同步默认开启必须 blocker。 | codex_dry_run_created |
| RT-008 | operation log runtime | planned | 关键业务动作写 operation logs，敏感字段脱敏。 | plugin-rental-core | `packages/plugins/plugin-rental-core/src/server/services/operationLogService.ts`, `packages/plugins/plugin-rental-core/src/server/collections/operationLogs.ts` | 是 | 是 | 将 operation log 写入挂接到合同、付款、押金、GPS 错误等动作。 | 每个关键动作可追溯，日志不泄露敏感字段。 | codex_dry_run_created |
| RT-009 | 权限检查 runtime placeholder | planned | 权限、角色和敏感字段校验须独立覆盖。 | plugin-rental-core | `packages/plugins/plugin-rental-core/src/server/permissions/permissionRegistry.ts`, `packages/plugins/plugin-rental-core/src/server/services/permissionFilterService.ts` | 是 | 是 | 下一轮补 Permission / sensitive field test stage。 | 权限阶段报告覆盖司机、车辆、付款、GPS token 等敏感字段。 | next_codex_task |
| RT-010 | 页面动作 runtime placeholder | pending_verification | 页面按钮/动作需要与 server action 对齐，不能绕过权限。 | plugin-rental-core, plugin-contract-documents, plugin-iopgps | `packages/plugins/*/src/server/actions/actionRegistry.ts` | 是 | 是 | 页面 / 菜单 / 区块初始化阶段补齐动作绑定清单。 | 页面动作可追溯到 server action，权限不可绕过。 | pending |
